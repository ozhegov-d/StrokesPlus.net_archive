#### Microsoft Azure Translator (+esc)
Azure Translator is a cloud-based machine translation service that you can use to translate text and documents using a simple REST API call. The service uses state-of-the-art neural machine translation technology.

If clipboard hasn’t changed → press Esc  
If clipboard has changed → translate (but if text length > 2500 characters, press Esc instead)  
